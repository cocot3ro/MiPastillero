<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.a23pablooc.proxectofct">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--TODO: android:icon="@mipmap/app_image", android:roundIcon="@mipmap/app_image"-->
    <application
        android:allowBackup="true"
        android:name=".PillboxApp"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/app_image"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_image"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProxectoFCT"
        tools:targetApi="31">

<!--        <service android:name=".notification.NotificationAction" />-->

<!--        <receiver-->
<!--            android:name=".notification.NotificationsService"-->
<!--            android:enabled="true" />-->

        <activity
            android:name=".ui.view.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>